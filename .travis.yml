language: node.js
node_js:
  - 6
cashe:
  directories:
    - "node_modules"
before_script:
  - npm install
script:
  - npm test
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master